# Vercel KV (Storage). Created automatically when you add KV to the project in the Vercel dashboard.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Local dev: copy this file to `.env.local` and paste values from Vercel → Project → Storage → your KV database → `.env.local` snippet.
